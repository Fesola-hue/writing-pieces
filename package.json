{
  "name": "aisha-onola-writing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "preview": "npm run build && wrangler dev",
    "sync:offscript": "node scripts/sync-offscript.mjs",
    "check": "node scripts/check.mjs",
    "lint": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check scripts/sync-offscript.mjs && node --check scripts/check.mjs && node --check scripts/seo-check.mjs && node --check public/site.js",
    "typecheck": "node scripts/check.mjs",
    "check:seo": "node scripts/seo-check.mjs",
    "test": "npm run lint && npm run build && npm run check && npm run check:seo",
    "deploy": "npm run build && wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
