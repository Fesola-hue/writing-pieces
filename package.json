{
  "name": "aisha-onola-writing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "preview": "node scripts/dev.mjs",
    "sync:offscript": "node scripts/sync-offscript.mjs",
    "check": "node scripts/check.mjs",
    "lint": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check scripts/sync-offscript.mjs && node --check scripts/check.mjs && node --check public/site.js",
    "typecheck": "node scripts/check.mjs",
    "test": "npm run lint && npm run build && npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
